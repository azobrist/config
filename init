#!/bin/bash
INIT=configured
if [ -f "$INIT" ]
then
	exit 1
fi

mkdir old
cd ~
FILE=.bash_aliases
if [ -f "$FILE" ]
then
	mv $FILE config/old/$FILE
fi
ln -s config/$FILE .

FILE=.gitconfig
if [ -f "$FILE" ]
then
	mv $FILE config/old/$FILE
fi
ln -s config/$FILE .

touch config/$INIT
