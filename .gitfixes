.bash_aliases:94: 	BRANCH=$(git st | awk '{for(i=1;i<=NF;i++)if($(i-1)=="On"&&$i=="branch")print $(i+1)}')
.bash_aliases:95: 	echo "Smart Commiting $BRANCH"
.bash_aliases:96: 	git diff --staged | diff-lines | sed /^.gitfixes/d > .gitfixes
.bash_aliases:97:+	git add .gitfixes
.bash_aliases:98: 	FILES=$(git diff --name-only --staged)
.bash_aliases:99: 	STR=$'files modified - add descriptions as needed or see .gitfixes for all modificationsn'
.bash_aliases:100: 	git commit -m "$STR$FILES"
